<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.lemphis.springbatchdemo.batch.mybatis.Mapper">

	<select id="selectFromSrc" resultType="me.lemphis.springbatchdemo.batch.jpa.Src">
		SELECT first_name AS firstName, last_name as lastName, email
		FROM src
		<if test="_skiprows != null and _pagesize != null">
			LIMIT #{_skiprows}, #{_pagesize}
		</if>
	</select>

	<insert id="insertIntoDest" parameterType="me.lemphis.springbatchdemo.batch.DummyItemDto">
		INSERT INTO dest(full_name, email)
		values (#{fullName}, #{email})
	</insert>

</mapper>
